/*package com.dipu.dpms.util;

import org.springframework.validation.MessageCodeFormatter;

public class MyMessageCodeFormatter implements MessageCodeFormatter {

	@Override
	public String format(String errorCode, String objectName, String field) {
		return errorCode + "." + objectName + "." + field;
	}
}
*/